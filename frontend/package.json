{
  "name": "neon-rps-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd /app/artifacts/rps-game && PORT=${PORT:-3000} BASE_PATH=/ pnpm exec vite --host 0.0.0.0 --port ${PORT:-3000} --strictPort"
  }
}
